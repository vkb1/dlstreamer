# Model export dependencies for deepstream_lpr_conversion
# Run once before starting the pipeline: pip install -r export_requirements.txt

# IMPORTANT: CPU-only PyTorch — must appear before any torch-dependent package.
--extra-index-url https://download.pytorch.org/whl/cpu

# OpenVINO Python version (pin to match DL Streamer runtime)
openvino==2026.2.0
nncf==3.0.0  # required for int8=True quantization

# Ultralytics YOLO (YOLOv11 license plate detection)
ultralytics==8.4.57

# HuggingFace model download
huggingface_hub

# PaddlePaddle models (OCR) — paddle2onnx → ovc conversion
paddlepaddle
paddle2onnx
onnx
onnxruntime
onnx_graphsurgeon
